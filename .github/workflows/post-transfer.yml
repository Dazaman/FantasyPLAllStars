name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    name: File Transfer

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@actiontest
    - name: Copy
      uses: actions/checkout@master -- /wiki/Test.md
